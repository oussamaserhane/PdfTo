'use client';

import { ScanText } from 'lucide-react';
import { createHoverIcon } from './create-hover-icon';

const ScanTextIcon = createHoverIcon(ScanText, 'ScanTextIcon');

export default ScanTextIcon;